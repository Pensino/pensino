<% title 'Editing TimeTable' %>

<% content_for :main do %>

  <%= render 'form' %>

  <%= link_to 'Back', {:controller => :courses, :action => :show, :id => @time_table.grid.course.id} %>
<% end %>

