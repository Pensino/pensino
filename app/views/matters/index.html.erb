<h1><%=t :matters %></h1>

<% @matters.each do |matter| %>
  <p>
  <%= link_to matter.name, matter %> </br>
  <%= matter.description %> </br>
  <%= matter.quantity_lessons %> Aulas <br />
  </p>
<% end %>

<br />
<%= link_to 'Nova Disciplina', new_matter_path %>

